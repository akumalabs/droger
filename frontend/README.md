# Droplet Manager Frontend

This frontend is built with Vite + React + TypeScript + Tailwind.

## Scripts

```bash
npm install
npm run dev
npm run typecheck
npm run build
```

## Environment

Create `.env` in `frontend/`:

```env
VITE_BACKEND_URL=http://localhost:8001
```
